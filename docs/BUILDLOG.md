# Build log — the development path (living summary)

> A compact, human-readable distillation of the diff2 development path: one entry per
> milestone, capturing the *why* and the *shape* of each change in a way that is hard to
> reconstruct from commit messages alone. **This is a living document — append new
> milestones here as they land** (newest after oldest).
>
> Division of labour between the docs (so nothing is written in the wrong place):
> - **git commit messages** = the DETAILED build log — byte-exact, one per logical change,
>   with real timestamps (CLAUDE.md §7 already mandates them as documentation). Ground truth
>   for *what exactly* changed.
> - **this file (`BUILDLOG.md`)** = the COMPACT summary of the path — the milestone-level
>   narrative distilled from those commits. Write here what a reader can't easily get by
>   scrolling `git log`.
> - **`CHANGELOG.md`** = RELEASES ONLY (Keep-a-Changelog; per-shipped-version notes). NOT
>   day-to-day development progress.
> - **memory `project-diff2-resume-point` + `docs/tasks/DIFF-EDITOR-V2.md`** = the LIVE
>   "where are we right now" pointer — the single source for resuming a session.
>
> Entries are stacked newest-after-oldest; later "SUPERSEDES / LIVE POINTER" lines correct
> the ones above them — read top-to-bottom as the order the milestones actually landed.

---

**The diff2 code is the V2 representation rewrite — the SOLE diff-editor implementation.** The early §1/V1 model (`editor-model`/`joined-doc`/`diff-pane`/`decorations`/`markers`/`chunk-actions`/`exit-protocol`/§1 `history-log`/`history-replay` — flat CM6 doc + `Segment[]` + `\0/\1` sentinels) was a **discarded branch, removed from `src/` + `tests/`** (and from the specs — see the V1-purge line below). Model + interaction (CM6 doc + terminal-`\n` per ver-block + Inclusive RangeSet): `diff-model.ts`/`diff-structure.ts`/`diff-pane-v2.ts`/`diff-decorations.ts`/`diff-edits.ts`/`diff-selection.ts`/`diff-resolve.ts`/`diff-line-numbers.ts`/`diff-nav.ts`/`diff-toolbar.ts`/`word-level-diff.ts`/`diff-auto-resolve.ts`/`diff-clipboard.ts` + `diff-pane-owner.ts` (owner) + `history-log-v2.ts`/`history-replay-v2.ts`/`history-feed.ts` + `eol.ts` (bug-59); the panel/editor split adds `diff-detail-controller.ts`/`diff-editor-view.ts`/`editor-tabs.ts`. Commit/recovery/autosave (`exit-commit.ts` `commit7Step`/`recoverCommit`, `autosave-store.ts`, `cursor-*`, `onload-recovery.ts`, `recovery-dialog.ts`) + trash (`trash-*.ts`) are representation-independent. **Current live-state pointer = memory `project-diff2-resume-point` + the V2 spec [`docs/tasks/DIFF-EDITOR-V2.md`] (user-authored).** **Milestone (2026-06-19, branch `fix-diff-editor`, pushed @ `9ed0f6f`): absent-base conflicts (delete-vs-modify) + empty-resolution shipped** — synthetic-detector lists absent-base siblings; `commit7Step.baseCommitAction` does empty-base semantics (absent-at-start→delete / 0-byte-at-start→write 0 / had-content-emptied→`EmptyDeleteModal` Delete/Keep("\n")/Cancel); `done.json.deleteBase` + `Commit7Result.baseDeleted` (notice "Deleted" vs "Saved"); recovery delete-aware. Canonical detail: DIFF-EDITOR.md §5.0.g + §0.3.4, PLAN R3.3. **✅ MILESTONE — V2 interaction layer COMPLETE (2026-06-20, pushed @ `2c698a9`, device-verified):** the whole DIFF-EDITOR-V2.md interaction roadmap shipped — §2.2.13 auto-resolve (VANISH `ver1==ver2`) + split/shrink (scoped re-diff, `diff-auto-resolve.ts`), §2.2.12+§2.2.5 п.3 merge (Delete/Backspace separator, Ctrl+Y/Shift+Mod+K `diffDeleteLine`, select+delete, 2/3/4-group, multi-run cascade) with §6.1 caret (join-point / ver2-when-ver1-empty), §2.2.4 p5c Ctrl+A/group-spanning selection-delete (=§2.2.9 "neither"), §2.2.7 clipboard COPY/PASTE (`diff-clipboard.ts`: serialize/parse/materialize) + CUT (single shared `selectionSpansTerminal`), boundary bug-fixes + Ctrl+K. Architecture = "text + Ranges" (live filter computes the cascade once → records `(change, structure, caret)` → undo/redo+replay APPLY, never re-run diff2); recorded in DIFF-EDITOR-V2-ANALYSIS.md (§2.1 context-dispatch, §6.1 caret, §5 gate). diff2 988 unit-tests green. **✅ §0.5.5 CAROUSEL (history-log compaction) COMPLETE (2026-06-20, pushed @ `f967bba`, device-verified) — fixes bug-31/32:** (a) metric-fix — `assessHistoryV2.edits` counts only `newGroup` edits (= live undo depth; was counting every keystroke-block → "edits saved" grew to 162 vs real ~20); (b) `compactHistoryV2` (history-replay-v2.ts) — CONSERVATIVE (drops only dead groups: undone + redo-branch-truncated-by-a-later-edit, per the user's "10 edits→undo 7→+1→exactly 4"), pure, lockstep-proven on the real bug-31 log (428→68 blocks); `seq` now in the block checksum + `reseal`; (c) `rewriteHistoryAtomic`/`recoverHistoryRewrite` (history-rewrite.ts) — adapter-level bak-based atomic swap + onload recovery (in `recoverAutosaveDirs`); (d) REOPEN-trigger `compactSessionLog` runs before the Resume modal; (e) THRESHOLD-trigger {100 undo / 200KB cancelled} — `HistoryWriterV2` runs an injected `compactRunner` on its tail (race-free with appends) + resets seq; `cancelledBytes` was dead-wired (feed never passed `undoneBytes`) → `historyFeedListener` now measures an undo's full reverted span. diff2 1013 green. Canonical: DIFF-EDITOR.md §0.5.5. **✅ §2.2.6 п.7e KEYBOARD SELECTION COMPLETE (2026-06-22, branch `fix-diff-editor`, browser-verified) — fixes a large class of selection bugs that 4 rounds of green happy-dom suites MISSED:** the bugs all lived in keyboard Shift-motion (where the caret LANDS), which happy-dom cannot simulate — so "clipboard copies the whole group" etc. were Shift-motion-overshoot in disguise (native moveVertically preserved the column → overshot the collapsed diff-group → legalizer (correctly) made it whole-group → clipboard (correctly) fenced it). Fixed across three layers: (1) STRICT anchor-relative `legalizeSelection` (diff-selection.ts) — whole-group only when ≥1 char of the OTHER region is captured via half-open `[lo,hi)` (boundary `head==v2.from` = ver1-only, not whole; user-confirmed "start≠end at boundary"); (2) `selectionVertTarget` (diff-structure.ts) + `Shift-ArrowDown/Up` in `diffNavKeymap` — snap the head to the FIRST region boundary (v1.from/v2.from/v2.to), + §7e.ii.d/iii.c anchor re-base (start on an EMPTY ver → select nothing, caret re-bases to the next region); (3) single-ver clipboard/CUT/delete (`copyClipboardText`/`selectionDeleteSpec` via shared `singleVerSpan`) — a single-ver selection copies PLAIN `verContent` (protected terminal dropped, line `\n` kept) + deletes content-only (ver→empty), NOT the whole group. Forward + backward stadiality (ver1-only/ver2-only stepping → whole group) all browser-verified. **The observation tool: `harness/diff-pane-harness.{ts,html}` + `obsidian-stub.ts` — a browser-MCP Chromium harness (esbuild IIFE, `--alias:obsidian=`, served `python3 -m http.server 8731` from repo root, opened `http://host.docker.internal:8731/...`; `window.H`={mount,setCaret,setSel,sel,struct,copy,peek}); the `.js` bundle is gitignored.** This is the canonical oracle for ALL diff2 render/motion/caret bugs going forward (happy-dom = model-only). diff2 1055 green. Detail: memory `project-diff2-selection-model`. **Next (NOT a milestone): pixel-verify bug-34/33 bleed + bug-35 marker tint (harness screenshots / device); toolbar redesign ([[project-diff2-toolbar-redesign]]), Phase 7 History / Phase 8 Compare / Phase 9b Deleted modes, entry-points E4/E5/E6.**

**✅✅ DIFF-EDITOR V2 FEATURE-COMPLETE — feature set FROZEN (2026-06-27, `fix-diff-editor` PUSHED @ `1a928e3`). The CLAUDE.md diff2 paragraphs ABOVE are HISTORICAL build-log; THIS line is the current truth.** The whole DIFF-EDITOR-V2.md interaction roadmap + the surrounding UX shipped + device-verified; the user has declared the features complete (bugs will still be found/fixed; no NEW features except the one agreed below). Done since the milestone above: **§2.2.14 touch-only/read-only** (`editable=true`+`readOnly=false`+`changeFilter` rejecting input/delete userEvents — readOnly kills undo, editable=false kills shortcuts; `touchOnlyFacet`; Settings "Diff Editor" group, platform default mobile=true via `Platform.isMobile`); **§2.2.15 toolbar redesign** (`diff-toolbar.ts` — 2-row layout, back/Keep-Apply-Join-all, ↑/↓ conflict-nav + `Ctrl+[`/`Ctrl+]`, Undo/Redo, live `Conflicts:NNN` red>0/green=0, 3 per-session Compartment toggles Touch/Auto-focus/Diff-mode; interim `toolbar-conflicts.ts` DELETED; auto-focus default-true, focus-on-open rAF-deferred); **bug-8 char-level intra-chunk diff** (`computeWordDiff` diffChars + `CHAR_DIFF_BUDGET` word-fallback + Settings "Diff highlight mode" dropdown); **bug-50/51 trailing-`↵` diff** (`glyphDiffLine`, last EOL-less group only); **conflict file title → VIEW HEADER** (`getDisplayText` dynamic + direct `.view-header-title` DOM set; in-body title-row dropped, +1 editor row); **🔴 bug-56 fault-tolerant RECOVERY** — `autoResolveFilter` re-cascaded on REPLAY → doc divergence → `RangeError` → `mountReplayed` threw before `this.owner` set → dead toolbar/auto-focus; fixed by (1) `autoResolveFilter` skips `replayDispatch` (undo/redo-userEvent guard tried + REMOVED as inert — debit=credit proven WITHOUT it by `recovery-forward.test.ts`), (2) `replayHistoryV2` STOPS at the first un-appliable block (returns `stoppedAtError`, never throws → resume never bricks; base+sibling = ground truth, user re-resolves the safe-prefix tail), (3) pre-flight dry-run before the Resume modal → "NNN edits saved" = REAL recoverable count, 0 → silent fresh (drops broken `history.jsonl`); permanent monitoring logs `diff2 undo/redo` (debit=credit watch) + `diff2 replay STOPPED` + `diff2 dry-run/recovered/fresh-mount {docBytes,ms}` (perf). Tests: `tests/diff2/fixtures/bug56/` + `bug56-replay.test.ts` + `recovery-forward.test.ts`; full unit suite ~1893 green. **Live pointer = memory `project-diff2-resume-point`.** **OPEN ITEMS (not new features): (1) recovery-replay PERF** — replay = O(N×doc) decoration rebuilds, done twice; see `docs/tasks/TODO.md` perf chapter (options A skip-decorations-during-replay / B stripped in-memory dry-run / C reorder + round-trip-compaction missed-opportunity). **(2) entry-points** E4 file-menu / E5 deep-link / E6 post-sync-modal. **(3) History/Compare/Deleted** modes (type-defined in `events.ts`, not rendered). **THE ONE AGREED NEW FEATURE: a search panel** (`Ctrl+F`) — reuse the `@codemirror/search` engine (find/next/prev/highlight); a simple query language = space-separated words + quoted/bracketed phrases → regexp SearchQuery; **integration caveat the user flagged: `selectionLegalizeFilter` must NOT mangle a search-driven match selection** (gate it on the search userEvent). User to spec; not yet built.

**✅✅ SUPERSEDES the FROZEN line above as the LIVE POINTER — panel/editor SPLIT + Phase-1B persistence + device/engine fixes (2026-07-01, `fix-diff-editor` PUSHED @ `e60a695`).** The single diff2 view was split into a SINGLETON `DiffPanelView` (view-type `diff2-edit-view`, list-only) + a MULTI-TAB `DiffEditorView` (view-type `diff2-editor-view`, one per `base:sibling`), sharing ONE `DiffDetailController` (the detail engine — byte-identical through S2–S6, proving the seam). New files: `src/diff2/{diff-detail-controller, diff-editor-view, editor-tabs}.ts` + `recovery-dialog.ts` `EditorBusyModal` (inventory in the spec, not re-listed above). Row-click → open-guard (`openGuard`/`writeSetFor`, same-pair focus / partial-overlap dialog) → editor tab; `[←]` → `planBackNav` → reveal panel + scroll. **Phase-1B:** both view-types survive an Obsidian restart (`getState` strips a transient `openMode` → silent resume vs user-reopen modal); a restored editor whose conflict resolved between sessions detaches cleanly. **Device UI fixes:** diff-editor tab title (1B async-mount regression), `TokenExpiredModal` auto-dismiss, GitHub-token field select-all-on-focus, "Sync interval" hidden under "Manually", conflict-count badge on cold restart (#7 findAllConflicts recompute at layout-ready). **ENGINE (SYNC2 §7.8/§7.9):** adoption tie-break by the file's last-change date (not HEAD), canonicalize excludes `<configDir>/**`, and a MANDATORY `push-inflight` crash marker that ELIMINATES the one-device "conflict with yourself" (push→record gap). **Canonical detail:** `docs/tasks/done/SPLIT-PANEL-EDITOR-FEASIBILITY.md` (split/1B) + SYNC2.md §7.8/§7.9 (engine) + memory `project-diff2-resume-point` / `project-device-bugs-2026-06-30`. **✅ 1B + UI fixes DEVICE-VERIFIED (2026-07-01: restart content-survival [the edit survives a cold restart — silent resume, bytes checked], multi-editor restore, close-x→reopen modal, resolved-between-sessions detach, + all 5 UI fixes).** Only `TokenExpiredModal` auto-dismiss pends a real token expiry (~1wk, time-blocked not code); engine §7.8/§7.9/canonicalize are deterministic-unit-guaranteed (device is an optional spot-check). **✅ bug-59 CRLF/CR support DEVICE-VERIFIED (2026-07-01, PUSHED @ `5ce92ae`):** a Windows-CRLF (or old-Mac-CR) conflict used to throw `RangeError: Invalid position …` — the model hardcodes `\n` and CM6 strips `\r`. Fix `src/diff2/eol.ts` (`detectEol`/`toLf`/`restoreEol`/`commonEol`, priority LF>CR>CRLF): normalize to `\n` at EVERY model-entry (fresh mount, `readResumeSession`, `startSession` joinedDocSha, `classifyReopen` replay-gate); restore the session EOL (`meta.eol`) on EVERY vault write (`baseCommitAction` + `commit7Step` sibling + §5.0.e `commitUnchangedSide`/`commitToAlt`). Snapshots + all git-blob SHAs stay byte-exact/raw (Option B — no SHA-layer change). CRLF preserved byte-exact on write only when BOTH sides were CRLF (any LF side → LF). Compare mode (two write targets) deferred.

**✅ V1 PURGED FROM DOCS + panel-move + editor popout-move (2026-07-02).** DIFF-EDITOR-V2 is the SOLE diff-editor impl — the §1/V1 model was already gone from `src/`+`tests/`, and is now purged from the three specs: **DIFF-EDITOR.md** (−1541 lines — §1 «R7.7 core» model + §0.1–§0.4 §1→V2 migration contract + §7 V1-test-plan removed; §2.5 joinedDocSha / §2.6 history-format / §3.3 replay rewritten to V2 terms `serializeModel(buildModel)`/`VerRange[]`, no sentinels; §7 → pointer to the real `tests/diff2/` suite), **DIFF-EDITOR-V2-ANALYSIS.md**, **DIFF2_IMPLEMENTATION_PLAN.md** (R9.1 rollout banner-marked historical). Acceptance grep across all three → only deliberate «removed» banner statements remain. **Window arrangement (SPLIT-PANEL topic CLOSED, feasibility doc → `docs/tasks/done/`):** diff-panel MOVES to a split/window, staying singleton (onOpen guard inverted — keep `this.leaf`, detach older); editor popout-move = native Obsidian `moveLeafToPopout` reparent (SAME object, undo intact, log-proven); editor in-window split correctly REFUSED (duplicate-guard — would be a write-race).

**✅ Smaller diff2 items (2026-07-05 → 07-11, branch `diff2`, PUSHED through `be75dd9`).** Detail in the commits; distilled here so the path stays continuous: word-mode diff now highlights whole Cyrillic words, not byte-fragments (`50363dc`); bold the join quote header `> **Changes from …**` (`8c7f7b6`); neutral device/author names in test data (`98c3cbe`); **§24 pre-sync conflict modal is tracked-only + accurate copy** (`8502144`); **CLAUDE.md split** into this file + `docs/ARCHITECTURE.md` + `.claude/rules/*` so the rules stop drowning in build history (`a4d39dc`/`23863da`); toolbar **"Edit" toggle** — the positive-sense inverse of the internal `diffEditorTouchMode` (checked = editing enabled; desktop default on, mobile off), row-1 flattened to one wrap-flow; opt-in **"replace" checkbox** in the CM6 search panel (`be75dd9`) — the replace row is hidden until ticked AND the editor is editable.

**✅ Mobile diff-editor scroll UX — the hard piece (2026-07-12, branch `diff2` @ `f472695`, device-verified).** Fixes the Android soft-keyboard bug and builds a *predictable* mobile scroll model. Root symptom: the soft keyboard SHRINKS the Obsidian view (798→462px, measured — no overlay in portrait), which collapsed the `flex:1` editor body to 0 → blank content + reversed IME typing. New `src/diff2/mobile-header-clamp.ts` (editor-tab only, no-op on desktop): sizes the editor body so the outer `[toolbar + 16px tail + body]` scroll range lands exactly at the header bottom (editor never collapses), and **hard-clamps** that scroll leaving a 16px "tail" grab-handle so the toolbar can collapse fully yet always be dragged back. Layered fixes, each device-traced: **#3** restore the user's scroll position across keyboard open/close (closing the keyboard zeroes `scrollTop` BEFORE our `ResizeObserver` fires — so we re-apply the last real position; the toolbar stays where the user left it); **#4** `overscroll-behavior:contain` on `.cm-scroller` (CSS, `body.is-mobile`) decouples text scroll from the toolbar — text scrolling never nudges the toolbar at either edge (the user's explicit ask); the toolbar now moves ONLY by dragging the toolbar/tail; **#5** publish `--diff2-offfold` (how far the scroller hangs below the fold ≈ toolbar height open, 0 collapsed) so `.cm-content` padding lets the last lines reach the visible slice with the toolbar open, no excess gap collapsed; **#2/#2b** `.cm-content` bottom padding = navbar/safe-area base + off-fold, with the navbar base dropped while the keyboard is up. Two 16px grab-strips (`.diff2-detail-kbd-strip` under the toolbar, `.diff2-search-kbd-strip` at the search-panel bottom), toggled by search state. **Landscape + keyboard = DELIBERATELY NOT fixed in code (not a bug to re-open):** device-traced, the landscape keyboard OVERLAYS without shrinking the view AND is invisible to BOTH `root.clientHeight` and `visualViewport.height` (`vvH == winH == 411` captured mid-scroll with the keyboard confirmed open), so the last ~2 lines sit under the keyboard with no measurable height to compensate. A scroll-past-end padding was designed and then dropped; the agreed resolution (user) is to reach those lines via **Android's floating keyboard**. Portrait works fully across all modes. Diagnostic logging (`diff2 header-clamp`, behind the opt-in logger) is intentionally left in for future mobile work. Detail = commits `a4d39dc..f472695` + the thorough header of `mobile-header-clamp.ts`. **NEXT (flagged "later" by the user): diff-group marker-panel (`<<<<<<`/`======`/`>>>>>>`) LAYOUT rebuild** — change the layout-building philosophy so the panels behave predictably across screen widths, plus compactness and some visual polish (`MarkerWidget` in `diff-pane-v2.ts` + `.diff2-marker*` in `styles.css`).

**✅ Marker-panel LAYOUT rebuild + tab-revert/reopen-stuck fix (2026-07-12 → 07-13, branch `diff2`; rebuild committed+pushed @ `3cd1e3c`, the revert-fix device-verified on top).** The `<<<<<`/`=====`/`>>>>>` conflict panels now lay out **predictably and compactly at any width**, on a user-designed philosophy. Shape (`3cd1e3c`): every piece is CONSTANT width (monospace, fixed strings/labels, gutter grows only with digit count), so the panel is built as **monolithic flow-wrap atoms** (glyph / each button / device-label = `flex:0 0 auto`+`nowrap` in one wrap flow → a 5-char glyph never splits, the label wraps as a whole) and the mode is chosen by **exact arithmetic on measured intrinsic widths** (`marker-layout.ts` `chooseMarkerMode` — an 8-rung priority ladder, first-that-fits) rather than CSS breakpoints. **User-ratified priority ladder:** (1) everything on ONE LINE (label inline) ≻ (2) TEXT buttons over compact icons ≻ (3) NORMAL gutter offset over sliding left into the empty gutter zone — sacrifice the lowest first (slide → icons → wrap the label to its own 2nd line). Each button carries BOTH a `.diff2-marker-btn-full` (word + arrows) and a `.diff2-marker-btn-compact` (lucide `check`/`trash-2` glyph + ALWAYS-visible arrows) in its DOM, so switching modes is a pure `display` flip — the icon is inserted once, never re-`setIcon`'d on resize. An `@container`/generated-`<style>` CSS-only variant was built and then **rejected**: `@container` queries the root width but the panel needs the content-box `avail`, and the two drift by the vertical-scrollbar width (~15px) → wrong mode at the boundaries (a device broke a label to two lines where the arithmetic kept one). **The tab-revert/reopen-stuck fix (2026-07-13, device-verified):** the reported glitch was that any focus change (switching tabs/apps, running Sync) or reopening a diff-editor tab reset every panel to the default (text + normal-offset + oversized label) — the same conceptual root as the mobile-toolbar-shutter bug, *transient DOM state lost on re-render*. Cause: `3cd1e3c` set the two mode classes on `view.dom` (`.cm-editor`, CM6-owned), which a re-render/re-mount recreated without reliably re-applying them. Fix: move the mode classes to the **persistent outer root** `.diff2-edit-view-root` (our own element — it survives CM6's inner re-renders), so the **static CSS** re-applies the mode to freshly-rendered markers with zero JS; ONE class-set per document (the type is one for all panels). Precision is kept by running `chooseMarkerMode` on the **live** `avail`; width changes are handled with an **eager first widths-acquisition** plus a **120 ms debounce** for steady-state resizes (recompute only the settled width — which also breaks the flip↔scrollbar oscillation); a **guarded `update()` hook** that fires only while widths are still unmeasured (tab mounted hidden → offscreen measure reads 0) is the second, self-disabling recovery trigger for a hidden reopen. A severe UI-thread freeze seen mid-development was isolated (via a temp data-attr discriminator + a Chrome-headless harness) to *diagnostic* code — per-apply `getComputedStyle`/`querySelectorAll`, a `MutationObserver`, a leaked module-level `PerformanceObserver`, and an unguarded `update(viewportChanged)` storm — all removed; the shipped controller is cheap and inert once measured. Backlog (only if reopen-stuck ever recurs on device): cache the constant widths at module level so a hidden reopen needs a single `avail` read, not a full re-measure. Detail = commit `3cd1e3c` + the marker-controller header in `diff-pane-v2.ts` + memory `project-diff2-resume-point`.

**✅ Mobile toolbar-shutter position survives a tab switch (2026-07-13, branch `diff2`, device-verified).** The last remaining mobile issue: after scrolling the collapsible toolbar "shutter" partway down, switching to another in-Obsidian tab and back reset it to the top (desktop has no shutter, so it's mobile-only; app-switch and Sync did NOT reset it — only a tab switch). Device-traced via `mobile-header-clamp.ts`'s existing log plus a temporary `INSTALL`/`DISPOSE`/`seq` + `scroll→0` instrumentation: the `seq` stayed constant (NOT a re-mount — same conceptual family as the marker-panel tab-revert but a different mechanism), and the trace pinned it exactly — on tab re-show Obsidian collapses the view (798→0→798) and zeroes `root.scrollTop`, which fires a `scroll` event; `onScroll` then did `lastUserScrollTop = root.scrollTop` and **clobbered** the saved position (`scroll→0 {was:138}`) a beat before the re-show relayout could restore it. Fix: `lastUserScrollTop` is the *user's* intended position, so only a REAL gesture may write it — track an active touch (`touchstart`→on, `touchend`/`cancel`→off with a ~400 ms momentum tail; safe because the shutter range is only ~200 px) and gate the save on it. Belt-and-suspenders for the relayout↔zero ordering race (advisor-flagged): the same involuntary drop, detected in `onScroll` while the layout is valid (`cap>0 && scrollTop < lastUserScrollTop`), is **restored right there** rather than trusting a later relayout — so it holds even if a focus cycle were relayout-then-zero. Device log confirmed across two cycles: `lastUserScrollTop` survives the `view:0` collapse, and on return the position is re-applied (138 and 115 px both held); no clobber, `seq` unchanged. The temp instrumentation was removed; the permanent `#3` relayout log stays. **Follow-up (same mechanism, second reported glitch):** in touch-only mode, clicking the toolbar's [next conflict] up to the LAST conflict collapsed the shutter fully. Traced: `scrollToConflict` calls `view.focus()`, and for the last conflict the caret is below the outer fold → a native focus-scroll (Android ignores `preventScroll`) drags the outer root up to `cap`, collapsing the toolbar (touch-only only — edit-mode is already focused so `focus()` is a no-op). Two changes generalize the shutter invariant to "the outer scroll changes ONLY by a real scroll gesture, in EITHER direction": the involuntary-restore now fires on any non-gesture move away from `lastUserScrollTop` (`!==`, not just the down-to-0 `<`); and the gesture is a touch-**move**, not touch-**start** — a TAP on a toolbar button is a touch on `root` but not a scroll, and opening the gesture window on touchstart mis-saved the focus-scroll as the user's position (device-traced: tap [next]→last set `lastUserScrollTop` 9.7→199.7). Device-confirmed: `scroll-restore {from:200,to:72/1/139}` catches the up-scroll, the shutter holds, and tab-switch/drag are unaffected.

**✅ TODO §29 — mobile paste of a copied diff-group reconstitutes instead of inserting raw text (2026-07-13, branch `diff2`, device-verified).** On Android, copying a diff-group and pasting it back dropped the RAW fenced `` ```github-easy-sync `` text into the document instead of rebuilding the group (screenshot-mobile-10). Device-traced (a temporary module-level `setClipboardTraceSink` bridging the paste DOM path to the opt-in file log, mirroring `setWordDiffPerfSink`): **copy was fine** — the clipboard held the correct fence — and the break was order-dependent and deterministic (the user pinned it: *first* paste after a copy = plain text, *second* paste of the same clipboard = the group). The trace showed why: the failing first paste arrives as IME text input — `{beforeinput inputType:"insertText", hasClipboardData:false, data = the whole fence}` — NOT a `paste` ClipboardEvent, so the existing `diffClipboardPaste` domEventHandler (which reads `event.clipboardData`) never saw it and CM6 inserted the raw string; the working second paste fires a real `paste` event (Android syncs the WebView clipboard only by the second attempt). Since the fence text is already in the insert string, **no async Clipboard API is needed**. Fix: a new `diffClipboardInput = EditorView.inputHandler.of((view,from,to,text)=>…)` in `diff-clipboard.ts` — CM6's sanctioned hook for IME/beforeinput insertions (it reverts the raw DOM mutation and applies our transaction; chosen over raw `beforeinput.preventDefault`, which Gboard ignores while mutating the DOM anyway). It engages only when the inserted text parses as a fenced group (`clipboardHasGroup` — so ordinary typing/IME composition falls straight through; `parseClipboard` on a keystroke is O(1)), then reconstitutes via `pasteSpec` at CM6's supplied `[from,to]` — for which `pasteSpec` gained an optional `range?` param (defaults to the primary selection, which the IME may not have synced to the insertion point). Registered in `diff-pane-v2.ts` after `diffClipboardPaste`; the two are mutually exclusive per paste (a real paste event preventDefaults before CM6 reaches the input handler — trace-confirmed). `parseClipboard` already tolerates the fence's missing trailing `\n` (the IME drops it: 134 vs 135 chars). The final device-verify (clean build, trace stripped): first paste reconstitutes, second paste still works, normal typing unaffected, Edit-off read-only still blocks paste. Tests: `clipboard-paste.test.ts` +2 (explicit-range override, no-trailing-newline). All diagnostic trace removed. Detail = the `diffClipboardInput` header in `diff-clipboard.ts`.

**✅ TODO §32 — diff-editor "Loading…" state (2026-07-13, branch `diff2`, desktop-verified).** Opening a diff-editor renders the toolbar synchronously but loads the body asynchronously (read files → compact → classify → replay/resume → mount the CM6 owner), so a slow open (big diff / crash-replay) showed an inert default toolbar over a blank body that read as a freeze. `diff-detail-controller.ts` `mount()` + `styles.css`: the toolbar is now hidden **synchronously** at mount start (add `.diff2-detail-loading` before the first await → before the first paint, so the inert toolbar never flashes — the first attempt added the class in the 150 ms timer, which let the toolbar paint then vanish; user caught the flash), and a "Loading…" spinner overlay is shown only after a **150 ms grace** (so a fast/small open shows nothing → straight to the document, never a spinner flash; `revealReady` clears the timer first). `revealReady()` (clearTimeout + overlay.remove + class remove) runs in a single `finally` on the load try → always swaps out on success / error / any early return (done.json, stale-guard, cancel). The recovery **Resume modal** keeps the spinner behind it (revealReady is only in the finally, after the whole load incl. modal + mount; the 150 ms timer fires independently during the modal await). Used `classList` (not Obsidian's `addClass`) so the happy-dom `diff-detail-controller.test.ts` host works. The overlay is a `.diff2-edit-view-root` child (now `position:relative`), z-index 500 (above the markers' 250), separate from the `body` the owner mounts into; mobile-header-clamp installs post-mount (rAF, after revealReady) so it's unaffected. **KNOWN LIMIT (→ backlog, worker-outsource of the diff): the spinner is a CSS `transform:rotate` on the compositor/GPU thread, so it keeps spinning even while the main thread is BLOCKED by the synchronous `buildModel`/`diffLines` + `new EditorView` + decoration build (+ crash-`replayHistoryV2`) — i.e. Obsidian is unresponsive during the heavy compute; the spinner is cosmetic, not proof of liveness.** The real responsiveness fix (move `buildModel`/word-diff off-main into the existing cpu-worker; CM6 view creation + replay must stay on-main) is TODO Part II territory — a separate, measured, medium/large task. No unit test (ItemView/mount is host-only).

**✅ TODO §31 — read-only (Edit-mode OFF) must not block crash-recovery replay (2026-07-13, branch `diff2`, unit RED-GREEN).** A "funny bug" (both mobile + desktop): with the diff-editor's **Edit-mode OFF** (touch-only / read-only), a crash was NOT restored on reopen — the recovery replay silently did nothing, leaving the file unchanged. Cause: the §2.2.14 touch-only `changeFilter` (`diff-pane-v2.ts`) rejects `input`/`delete` userEvents to enforce read-only, but `replayHistoryV2` re-applies the recorded edits with a userEvent `input.type` — so read-only blocked the *replay*, not just live typing. The other filters that must yield to replay (`diff-edits`, `diff-auto-resolve`) already exempt the `replayDispatch` annotation the replay dispatches carry; the changeFilter was the one that didn't. Fix = one line: `if (tr.annotation(replayDispatch)) return true;` before the userEvent check (+ import). Safe because the annotation rides ONLY replay/recovery dispatches, never live user edits — and undo/redo already passed (they carry undo/redo userEvents), so read-only for the user is unchanged. Tests (`recovery-forward.test.ts`, RED-GREEN verified by temporarily disabling the fix): recorded edits REPLAY into a touch-only view (crash restored while Edit-mode OFF), and a LIVE user `input.type` in a touch-only view stays blocked (read-only not weakened).

**✅ TODO §33 — ver1 marker label wraps ABOVE, not below (2026-07-13, branch `diff2`, desktop-verified).** In the marker-panel layout, when the device label no longer fits inline it wraps to its own line. For the `<<<<<` (ver1 / top) marker that put the label BELOW the glyph+buttons — between `<<<<<` and `=====` — where it read as ver1-block text (especially confusing for an empty ver1). Fix is pure CSS: `.diff2-marker-top { flex-wrap: wrap-reverse; }` — `wrap-reverse` stacks the wrapped flex line ABOVE line 1, so the label lands on top (`(Local)` / date over `<<<<< [buttons]`). No effect on the single-line case; uniform for every group (the marker-panel one-mode-per-document rule). `>>>>>` (ver2 / remote label) intentionally keeps wrapping downward — §33 is ver1-only. No unit test (flex geometry is browser-only; happy-dom can't measure it).

**✅ History diff-editor: mobile keyboard-shrink white-screen fixed (2026-07-13, branch `diff2`, device-verified).** On Obsidian Mobile with Edit-mode ON, opening a HISTORY diff-editor blanked to a white screen (the Android keyboard shrinks the view → the `flex:1` editor body collapses to 0) — the exact bug `mobile-header-clamp.ts` was built to fix, and which the CONFLICT editor no longer has. Root cause: `diff-editor-view.ts` has two mount paths, and only the conflict one wired the clamp — `tryMount()` did `controller.mount(...).then(() => rAF(installMobileHeaderClamp))`, but `tryMountHistory()` forgot it (plain `void controller.mount(...)`), so a History editor had no toolbar shutter and no keyboard-shrink compensation. Confirmed from the device log (a `fresh-mount {history:true}` was followed by ZERO `header-clamp` lines, unlike the conflict `{history:false}` case) and `adb logcat` (no JS throw → a layout collapse, not a crash). Fix: install the same clamp after the history mount. Pre-existing since History mode shipped — surfaced now, unrelated to the §32/§33 work in the same session.

**✅ TODO §35 — consistent "GitHub token expired" handling (2026-07-15, branch `diff2`, 6 commits `e8ba373`→`ab9f486`, unit-green + desktop-deployed; phone verify pending on the user).** The `token_expired` marker was inconsistent: set on a 401/403 but **auto-cleared on the very next successful GitHub call**, so it flickered and the UI never settled. Reframed (with the user) around a single ground truth — the Settings config, NOT a later call:
- **Sticky latch** (`e8ba373`, `token-expired-flag.ts`): a success NEVER clears the marker. The only clear paths are deliberate user actions — editing a Remote-Repository field (token / owner / repo) or a SUCCESSFUL "Test connection" probe (the one live re-check independent of the marker). `note()` sets-only; `classifyAuthOutcome`→`authErrorKind`.
- **Pre-flight gate + origin fork** (`a04d524`): every GitHub-touching surface (`sync`/`syncAll`/`uploadOnly`/`syncCurrentFile`/`backgroundDrain`, and later the history load) checks `isExpiredCached()` BEFORE the network. USER paths (ribbon/menu) → the recovery modal, UNTHROTTLED (the once-per-hour throttle is gone — it re-broke the "close [X] → next Sync stays silent" bug; a single open-flag stops duplicate stacking via a new modal `onClosed` hook). AUTOMATIC paths (interval/startup, threaded via `sync(false,{background:true})`) → a brief "Sync skipped: token expired" Notice (2s), never the modal. Because `AuthError` is non-retriable AND the gate blocks the retry, a repeat Sync "finishes fast" with no failing call — killing the "Failed to get branch head sha, status 401" toast by construction; the first-401 user path also suppresses that raw toast.
- **401/403 classification, persisted** (`2f73f51`): GitHub can't split "expired" from "wrong token" (both 401 "Bad credentials"), but 401 (invalid/expired → new token) vs 403 (valid, missing scope → re-scope) ARE different fixes. The marker FILE now stores the class tag (`invalid`/`scope`) instead of a timestamp; `init()` restores it (legacy ISO content → `invalid`); `tokenExpiredMessage(kind)` is the single copy source for the Settings card + modal. The Settings "GitHub sync status" card is now ENTIRELY marker-driven for auth (fixes the reload regression where it showed neutral "Sync idle" while the marker persisted — in-memory `DrainStatus.lastError` is null after a reload and a gated Sync records no error); non-auth errors still show `lastError` directly. Status-menu greys **Sync All** + **Pull from repo…** while expired (the gate would just short-circuit them).
- **Uniform red UI + startup restore** (`64716a5`): the status-bar "GitHub" word + the sync ribbon icon turn red (tooltip "Token expired") via a `TokenExpiredFlag.onTransition` hook → `refreshTokenExpiredUI()`, fired on every real set/clear AND once at `onLayoutReady` so a cold start with a persisted marker paints red immediately.
- **History load = full gate participant** (`d453a61`): `loadHistoryVersions` takes `isTokenExpired()`+`noteAuthError()` — a latched token skips the network (local-only + `tokenExpired:true`), a first-time 401 latches the marker; the view prefixes "TOKEN EXPIRED! " to the "Couldn't load GitHub history…" warning. (Deleted / Phase 9b inherits when it grows a GitHub load.)
- **Shorter mobile modal** (`ab9f486` + two device-driven follow-ups): `Platform.isMobile` branches the recovery modal — a phone gets a shorter title + the one-line class message, dropping the desktop intro + 3-step list. The three buttons ([Open GitHub token page] CTA, [README], [Open settings]) are the SAME on both platforms (`c39bf72` — the token page is needed on mobile too, it's in Settings' token-help box as well). On a narrow phone the buttons overflowed a single non-wrapping row (the first was pushed off-edge), so the row now wraps + centers (`005ec7a`, `.github-easy-sync-token-modal-buttons` → setting-item column + control `flex-wrap`/`justify-center`; needs styles.css deployed with main.js). Both device-verified by the user. §35 pushed to origin/diff2 @ `005ec7a`.